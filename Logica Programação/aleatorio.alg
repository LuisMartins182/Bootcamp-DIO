algoritmo "para"

var
n,aleatorioMax, aleatoriomin, incremento:inteiro
inicio
repita
aleatorio 1,100
          leia(aleatorioMax)
          leia(aleatorioMin)
aleatorio off
ate aleatorioMax > aleatorioMin
limpatela
aleatorio 1,5
leia(incremento)
aleatorio off

para n de aleatoriomin ate aleatoriomax passo incremento faca
     escreval(n)
fimpara

fimalgoritmo
